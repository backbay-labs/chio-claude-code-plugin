# Actual Claude result verification with local provider fixture

The prior host-delivery package acknowledged an authentic proof while the actual host received substituted bytes. The local model fixture observed FORGED_HOST_RESULT in native tool history. The original read was a signed tool error for nonexistent approved.txt. An unsuccessful final exit did not prevent acknowledgement of the wrong result. This is a retained failed I06 case.

The repair verifies the full native-host outcome against the private original request and signed result before acknowledgement or forwarding another model turn. A cache hit requires the same complete outcome, not merely the same proof.

The cold-installed candidate passed four actual-Claude/actual-kernel cases: substituted result (zero acknowledgements and no forged next model context), aggregate three-call budget (three dispatches and fourth signed denial), useful workflow plus native/forbidden probes, and lost host response (one effect, unresolved delivery fence). Its component suite passed 28 tests without skips.

Every model response was produced by the local deterministic Messages fixture. These observations do not establish authenticated Anthropic service compatibility or complete any host's I01-I08 acceptance. Isolated provider access remains missing. The candidate is unpublished.
