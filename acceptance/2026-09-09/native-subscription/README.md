# Claude native subscription evidence

The current r5 archive is `0dd0d906fc34b3ac7d09e3b7f6cdee9f13f511731b25ec761feca7172c9b1158`, built from runtime source `c91cd8163ea8b13c66214397bd132bc4f4cd17bf`. It uses Claude Code 2.1.267 (binary SHA-256 `a681f3008f0050029aeebcab3af51bb6a55ddeb625a3af3141a4416d43cd2558`), plugin/bridge 0.3.0, SDK 0.1.1-rc.1, and kernel SHA-256 `33dd1dea21a4ca5ecddeab4f30f6b06b0b90c513f0987aef552b0633d9da1e25` on macOS 26.4 arm64 with Node v25.5.0. Full identities and limits are in [artifact.json](artifact.json). Full I01-I08 acceptance and publication remain open.

[The current case index](current-r5/CASE-INDEX.json) records 18 passing native lifecycle scenarios, 33 passing shared driver suites, and the separate finalized capability-expiry suite. The shared matrix contains 44 case observations across 34 suites. Expected nonzero launcher exits establish refusal or truthful uncertainty for those cases; they are not failed test-driver exits. Startup refusals can end before provider inference. Cases reaching inference use the actual native Claude subscription and `claude-sonnet-5`. Resource content and audit dispatch rows are observed separately from the host's answer.

The lifecycle cases cover useful work, aggregate budget exhaustion, cancellation, network refusal, journal failure before dispatch and after effect, result substitution, response loss, gateway crash, SIGTERM recovery, missing components, silent MCP omission, malformed/timed-out/crashed initialization, upgrade and removal. The shared matrix covers forbidden file operations and aliases, secret access, seven approval stages, revocation and in-flight revocation, credential and actual capability expiry, wrong caller/session/resource, scope escalation, kernel absence/interruption/malformed responses/timeouts, receipt/request/signer substitution, retained-identity resume fencing, signed owner reconciliation, and concurrent-owner exclusion. Source snapshots, exact commands, raw host output, signed evidence, operator recovery observations and independent resource observations are retained beside each case.

Some model-directed denial cases use the provider's declared `tool_choice` to obtain an actual attempted call. The retained tool-choice logs expose that selection; the harness does not manufacture assistant arguments or results. The 27 supplemental boundary assertions and one supplemental parallel scenario use local fixtures and are recorded separately. The parallel scenario observes one completed action, one blocked action, one resource dispatch and overall exit 3. It does not prove two parallel calls execute successfully.

The r4 [pre-dispatch journal failure](historical-r4/cutpoints/results.json) and [incorrect successful parallel outcome](historical-r4/parallel/summary.json) remain recorded as failures. Their corresponding r5 cases now pass, but all r4 successes remain historical too. The `current-r3-*`, `pre-final-*` and original top-level transcripts retain their older artifact identities. The original r3 summary is preserved in `current-r5/provenance/previous-r3-*`. The unchanged [readiness r4 provenance](readiness-r4-provenance/source-delta.json) records a packaged, then-uncommitted nonruntime driver; it must not be described as a clean-commit-only artifact.

The [archive manifest](current-r5/provenance/archive-manifest.json) maps copied regular files to source paths and hashes, and lists excluded disposable consumer installations, npm caches and symlinks. Symlinks were not followed. Private authority and provider directories were not copied. [The credential scan](current-r5/provenance/archive-secret-scan.json) checked known provider, operator, session and npm publisher credentials internally without printing their values. `SHA256SUMS` covers all retained native-subscription files except itself.

The [cold installation record](current-r5/provenance/installation.json) identifies the empty-cache offline npm installation and installed launcher/gateway hashes. It was assembled after the successful tool invocation; the record states that provenance limitation. This evidence does not claim public availability. Kernel storage faults are a separate required lane: a bridge journal fault cannot establish kernel-store behavior. Final gate reconciliation, compatible artifact publication and any remaining release checks belong to the program acceptance record.
